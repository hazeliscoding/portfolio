/**
 * Facts about the operator that the chrome states out loud.
 *
 * `OPERATOR_STATUS` renders in the top system bar on every route and in the
 * boot overlay's log. It was hardcoded in both places, which made a public
 * statement about Hazel's availability a two-file edit to change. It is one
 * word here instead.
 */
export const OPERATOR_NAME = 'HAZEL GRANADOS';

export const OPERATOR_STATUS = 'OPEN TO CHAT';
